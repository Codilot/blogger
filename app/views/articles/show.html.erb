<% provide(:title, @article.title) %>

<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <!-- show article -->
      <h1>
        <%= @article.title %>
      </h1>

      <strong><%= @article.created_at %></strong>

      <h4>
        <%= @article.teaser %>
      </h4>

      <% if @article.image_url != nil %>
        <%= link_to image_tag(@article.image_url, class: "img-rounded article_image") %>
      <% end%>

      <p>
        <%= @article.text %>
      </p>

      <!-- show comments -->
      <h2>Comments</h2>
      <%= render @article.comments %>

      <!-- add comment form -->
      <h2>Add a comment:</h2>
      <%= render 'comments/form' %>

      <div class="btn-group">
        <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-default" %>
        <%= link_to 'Back', articles_path, class: "btn btn-default" %>
      </div>
    </div><!-- col-sm-8 -->
    
    <div class="col-sm-4">
      <h2>Read also</h2>
      <div class="random-item">
        <% if @random_article.image_url != nil %>
          <%= link_to image_tag(@random_article.image_url, class: "img-responsive img-rounded") %>
        <% end%>
        <h2><%= @random_article.title %></h2>
      </div>

      <div class="article-list">
        <% if @articles_latest != nil %>
          <% @articles_latest.each do |article| %>
             <h2><%= article.title %></h2>
          <% end%>
        <% end%> 
      </div>
      
    </div>


  </div><!-- end row -->
</div><!-- end container -->